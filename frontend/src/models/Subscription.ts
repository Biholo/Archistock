interface Subscription {
    id: number;
    name: string;
    size: number;    
    price: number;   
    features: string; 
    duration: number;  
    createdAt: string;
    updatedAt: string;
}

export default Subscription;